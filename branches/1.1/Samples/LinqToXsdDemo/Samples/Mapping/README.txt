This sample exercises a baseline XML programming scenario:
Consume/produce XML "messages" relative to a business object model.

The XmlObjects solution is compared with alternatives.
- Use of plain LINQ to XML
- Use of VB9's XML integration
- Use of DOM
- Use of DataContract
